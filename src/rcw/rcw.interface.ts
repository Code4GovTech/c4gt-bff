export type CandidateJSON = {
  id: string;
  name: string;
  email: string;
  did: string;
  credentialDID: string;
  product: string;
  project: string;
  organization: string;
  credential: any;
};
